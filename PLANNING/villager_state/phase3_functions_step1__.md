The new section appends two additions to `game_types.py` — a `total_minutes` property on `CraftableItem`, an `is_away` property on `ActionCategory`, and a `StatName` literal type alias — plus the full `VillagerState` public API for `villager_state.py`. Two functions were absent from the earlier design-doc Core Functions section: `is_over_encumbered` and `can_fit`, both needed by Action System for encumbrance gating and exploration truncation.