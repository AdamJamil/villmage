That's the only substantive finding. The subsystem is a simple static data store and the spec is explicit about its domain — no dev input is required to resolve anything in `character_canon`'s scope. The single ISSUE is a documentation inaccuracy in the `Profession` enum's docstring: it says profession tags are used "to gate exploration types" but `GATHERER` doesn't gate access — it only waives the 4× speed penalty, which is a timing modifier, not an access lock. `BUILDER` also has no effect at all, making the blanket "gate" framing misleading for two of the six values.